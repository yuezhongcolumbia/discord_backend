query guild membership:
docker compose exec db psql -U discord_user -d discord_db -c "SELECT guild_id, user_id, role FROM guild_memberships;"


generate the migration:
python -m alembic revision --autogenerate -m "create channels table"

apply the migration:
python -m alembic upgrade head

start the application:
fastapi dev app/main.py   ctrl + c to terminate
python -m  app.message_persistence_runner
python -m app.message_semantic_runner

change terminal env
& C:\dev\discord\.venv\Scripts\Activate.ps1


dev:
	user: 11111111-1111-1111-1111-111111111111
	guild: 7a7d2551-76d4-4a31-a028-f81b9910b270
	channel: 7316013f-90f9-4e25-9d60-4fb61b8a5348

pagination:
	1. situation a: everything you need in current bucket. return next_cursor if has more
	2. situation b: spill over to previous bucket(there could be no previous buckets available)
	in general: 
		1. query limit + 1 message(+1 is for checking if there is more message left in the entire channel), use +1 to decide whether to return a cursor for the final return 
		2. what does the while loop does:
			collect msg from current bucket
			if want more msg collected, update current bucket
